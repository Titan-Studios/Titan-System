
const talkedRecently = new Set();


module.exports = {
    commands: 'say',
    minArgs: 1,
    expectedArgs: '<message>',
    permissionError: 'You cant use that command!',
    callback: (message, arguments, text) => {

        const text1 = arguments[1]
  
  

    },
    permissions: [],
}